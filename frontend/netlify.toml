# Netlify: set "Base directory" to `frontend` in site settings (or deploy from this folder).
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback for React Router (client-side routes)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
